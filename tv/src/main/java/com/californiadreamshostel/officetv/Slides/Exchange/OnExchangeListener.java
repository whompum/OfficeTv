package com.californiadreamshostel.officetv.Slides.Exchange;

import android.support.annotation.NonNull;

public interface OnExchangeListener {
    boolean onExchange(@NonNull String newId, int newPos);
}
